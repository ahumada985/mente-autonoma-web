{
  "version": 2,
  "buildCommand": "prisma generate && npm run build",
  "installCommand": "npm install && npx prisma generate",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    },
    {
      "src": "api/chatbot_completo.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/chatbot/(.*)",
      "dest": "api/chatbot_completo.py"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}